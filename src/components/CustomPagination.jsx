import React from "react";
import { testimonials } from "../constants";

export default function CustomPagination({ index, img }) {
  return (
    <div className="w-[48px] h-[48px] rounded-full">
      {index}
    </div>
  );
}
